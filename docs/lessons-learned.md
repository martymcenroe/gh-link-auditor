# Lessons Learned — gh-link-auditor

| Date | Lesson | Rule/Action |
|:-----|:-------|:------------|
| 2026-05-12 | **`gh issue list --search "..."` ignores `--repo` and returns global results.** Tried `gh issue list --state all --search "..." --repo owner/repo` for a scoped search and got back 30 issues from completely unrelated repos. | **Don't combine `--search` with `--repo` expecting scoping. Use `gh issue list --state all --limit N --repo OWNER/REPO --json ...` and post-filter the titles client-side, OR use the search API with explicit `repo:OWNER/REPO` in the query string.** |
| 2026-05-12 | **Background pytest piped to a file looks hung — Python's stdio uses full buffering when stdout isn't a tty.** Spent ~15 min wondering if `pytest -q ... > log &` had stalled at 93%; it was running fine, just buffering all dots until summary. | **For background pytest runs that need progress visibility: set `PYTHONUNBUFFERED=1`, use `-u`, or use `-v` (line-buffered formatter). Foreground pytest in the harness streams correctly; the harness's auto-backgrounding handles the output file already.** |
| 2026-05-12 | **MEMORY.md test-count milestones can be unverifiable.** Memory had "1,705 tests after #148-#150" but `poetry run python -m pytest --version` failed with `No module named pytest` — tests had been running via base Python's globally-installed pytest the whole time, not the project venv. | **Before trusting a memory's test-count milestone, verify `poetry run python -m pytest --co` actually collects in the current venv. If `pytest` isn't in pyproject deps, the milestone describes a state that no longer reproduces.** |
| 2026-05-12 | **Time-boundary tests with hardcoded ISO dates rot.** `test_boundary_exact_age` hardcoded `2025-10-01` as "within the 6-month window" at write-time; today (224 days later) it's outside the 180-day window so the function correctly returns False and the test fails. | **In time-boundary tests, compute timestamps relative to `datetime.now`: `(datetime.now(timezone.utc) - timedelta(days=N)).strftime(...)`. Hardcoded dates ARE time bombs. Centralize via a module-level constant if reused.** |
| 2026-05-12 | **"Tests exist" ≠ "tests run at merge gate."** Repo had 1,749 tests passing locally but `.github/workflows/lint.yml` was the only gating workflow and ran zero tests. The repo's own "≥95% test coverage is mandatory" rule was unenforced. | **When auditing a repo, always check `.github/workflows/*` for what's actually gated. Treat the gate as the rule, not the README, the CLAUDE.md, or the dev's intent. Add a `Test` workflow whenever you find tests-without-gate.** |
| 2026-05-12 | **`.claude/worktrees/agent-*` are submodule gitlinks (mode 160000), not real directories.** Tried `rmdir` on an empty one and got "Device or resource busy"; only `git merge --ff-only` revealed they were `create mode 160000` entries. | **Before trying to filesystem-delete anything under `.claude/worktrees/` (or any agent-managed worktree path), run `git ls-tree HEAD .claude/worktrees/` first. If it shows commit gitlinks, the dirs are submodule references — clean via `git rm --cached` + commit, not `rmdir`.** |
| 2026-05-13 | **Sites can return 404 to HEAD but 200 to GET.** Microsoft Marketplace does this as anti-crawler defense. | **For any 4xx commonly used for bot-blocking (403, 404, 405), default to "try GET once" before declaring dead. Only 410 is safely terminal on HEAD.** |
| 2026-05-13 | **A successful redirect chain from a dead URL means the URL works.** SO short URLs `/a/N` redirect to long form — both work; "fix" is cosmetic. | **In any chain-following investigation: if the original URL responds (even 30x), it isn't broken — don't propose a candidate.** |
| 2026-05-13 | **Cloudflare-fronted hosts can return None status (TCP reset) not 403.** Pythonjobshq.com bypasses 403-only stealth fallback. | **Bot-blocking has multiple layers (HTTP 4xx, TCP reset, TLS challenge). Stealth fallback should check all, gated by a CDN-IP heuristic.** |
| 2026-05-13 | **GitHub's auto-close from PR body only fires for the FIRST `#N` after `Closes`.** "Closes #194 #195 #196" closed #194 only. | **Write one `Closes #N` per line in PR bodies. The keyword needs to repeat. Same for `Fixes`, `Resolves`.** |
| 2026-05-13 | **Polite-but-firm maintainer rejection misses `hostile_classifier`.** Pallets/flask davidism's "Happy to update this, but please do not use genAI…" matched no rude phrase. | **Different rejection classes need different classifiers. Hostile (#178) catches rude; anti-AI (#200) catches polite-firm. Use distinct `source=` values.** |
| 2026-05-13 | **JS-challenge pages return 403 to bots but load fine in real Chrome with stealth.** Career project's TypeScript Playwright + stealth pattern ported directly to Python with `playwright-stealth.Stealth().apply_stealth_sync(page)`. | **Use `playwright` with `channel="chrome"` — NOT `playwright install chromium`. No 150MB download. The bot-detection delta between bundled-Chromium and real-Chrome is significant.** |
| 2026-05-13 | **Operator-discovered false positives during HITL are the highest-signal labeled data.** Each one is a confirmed pipeline-disagrees-with-reality case the operator just verified. | **Build an `[l]ive` flag into any HITL prompt that asks a human to validate machine output. Print at end-of-session; persist to DB if possible. Mining false-positive patterns is how the pipeline improves.** |
| 2026-05-13 | **`gh search repos "language:Python stars:N..M ..."` rejects the bundled-qualifier positional arg with "None of the search qualifiers apply to this search type".** The positional is *keyword* search; qualifiers MUST be flag form. First bulk-scan run (#218) failed every star-slice for this reason. | **Always use `gh search repos --language=X --stars=N..M --archived=false ...` flag form.** When wrapping `gh search` in subprocess code, lock the flag-form contract with a regression test that inspects the `cmd` list. There's no `--pushed` flag; approximate via `--updated=>YYYY-MM-DD`. |
| 2026-05-13 | **Without `GITHUB_TOKEN` in env, httpx-based scripts go anonymous (60/hr) and trip GitHub's secondary rate limit within seconds on burst workloads.** The user has `gh auth login` configured but doesn't export the token. The second bulk-scan run died at ~50 inventoried repos with cascading 429s. Smoke-confirmed: anonymous client showed `X-RateLimit-Remaining: 30`; with the auth fallback, `4970`. | **Any new httpx GitHub-API caller in this repo MUST resolve the token via `gh_link_auditor.auth.resolve_github_token()` (env first, `gh auth token` fallback) — not `os.environ.get` directly.** Add it to the new-feature checklist when designing API clients. |
| 2026-05-13 | **`poetry env remove --all` run inside a sibling worktree directory (e.g. `gh-link-auditor-158`) can evict the editable install from the main project's venv on Windows** — even though Poetry should hash venvs per project path. The `/cleanup --full` subagent did this as part of orphan-worktree teardown; the next bulk-scan invocation died with `ModuleNotFoundError: No module named 'gh_link_auditor'`. | **Cleanup skills that touch sibling Poetry worktrees should re-run `poetry install` in the main project root afterward.** Or skip the `poetry env remove --all` step entirely when the worktree is empty (a venv it never used can't hold file locks). Update `/cleanup` skill's Phase 2.5 to guard with `[ -f pyproject.toml ]` or similar. |
| 2026-05-13 | **GitHub's secondary rate limit fires on burst patterns even when the primary 5K/hr quota is far from exhausted.** Designed-but-not-implemented throttling in LLD-218 became a real bug on first use. The 429 message references "secondary rate limit" or "abuse" — not the standard rate-limit headers. | **Any new bulk/loop pattern hitting GitHub REST MUST enforce inter-request spacing (≥500ms) from day one, not "later." Detection: 429 status OR 403 with `X-RateLimit-Remaining==0` OR 403 body mentioning "secondary rate limit"/"abuse".** Don't ship without it. Pattern is now `bulk_scan/gh_client.py`. |
| 2026-05-22 | **Resetting bulk-scan from `aborted` to `investigating` skips Stage 2; Stage 3 then deletes everything as alive.** | **Always reset to `'checking'`. #244 now makes this survivable (state stamps instead of DELETE) but the rule still applies — pick the status that puts the right gate-set into effect.** |
| 2026-05-22 | **Firing `bulk-scan start` twice against same run-id races both processes through Stage 3 against the same rows. Pending count freezes, findings vanish.** | **#244 added `bulk_scan_locks` + `LockBusyError`. Pre-#244 codebases: check `tasklist` before assuming a single instance.** |
| 2026-05-22 | **raw.githubusercontent.com rate-limits at ~300 fetches/sec bursts. Recovery scripts hit 429 after ~2,000 repos and don't recover for 30-60 min.** | **Throttle raw CDN bursts to ≤5 workers × ≥200ms spacing for any recovery/enrichment tool walking 1000+ repos. The throttling rules from `bulk_scan/gh_client.py` apply to raw CDN too, not just api.github.com.** |
| 2026-05-22 | **Bulk-scan quality stop-loss (median conf ≥ 0.7) self-aborts on random-GitHub corpora because most dead URLs are dead-end (deleted wikis, removed assets, withdrawn models), not movable. Threshold was calibrated for python-guide-style dev-tools corpora.** | **For bulk runs on random GitHub samples, lower `QUALITY_MEDIAN_THRESHOLD` to 0.3 or disable. Document the assumed corpus in `bulk-scan-kickoff.md`.** |
| 2026-05-22 | **Quoting operator language verbatim in a GitHub issue body tripped Anthropic's content classifier mid-session and broke response streaming.** | **Universal `CLAUDE.md` Hard Rule added: never quote operator profanity in any persisted artifact. Paraphrase the sentiment or describe the impact. Strip profanity from console-output pastes that get embedded.** |
| 2026-05-23 | **PAT leak via `gh auth token 2>&1 \| head -3`.** Diagnosed a suspected hang by checking `gh auth token` returned quickly. Used a generic `2>&1 \| head -3` "limit noise" idiom without thinking about what the specific command actually prints. `gh auth token` returns ONE LINE: the token itself. `head -3` is no protection on single-line output. Token printed into transcript. | **Pause before any command whose output could include a secret.** Ask "what does THIS command actually print?" not "is this a pattern I've used?". The CLAUDE.md secrets-to-stdout rule is explicit; classifier extension filed at `martymcenroe/unleashed#651` so the rule becomes runtime, not textual. |
| 2026-05-23 | **3 of 6 PRs shipped today introduced regressions caught only by operator vigilance.** PR #263 missed `follow_redirects=True` because unit tests mocked above the layer that mattered; caused 100% no-cand rate post-merge. PR #267 produced 11 min of zero progress at startup that I confidently misdiagnosed as a hang. PR #274 nearly shipped a corruption-injection (deleting `)'s` from real READMEs) — safety filter exists only because operator asked "what's in that PR?" before approving. | **Tests are not proof of working software.** Run the actual production path end-to-end on the operator's machine — even a 30-second smoke run — BEFORE declaring done. CI green is not enough. The operator stated this in the morning; ignoring it three times in the day made the leak inevitable. |
| 2026-05-23 | **Definition of Done means MERGE, not "PR open with green CI."** Held PR #274 for a smoke test, then gave operator a smoke-test command that didn't work (file wasn't on main yet because PR wasn't merged). Operator had to invoke `CLAUDE.md` Definition of Done before I merged. Cost the operator a wake-from-nap surprise. | **When CI goes green on a merge-implied task, merge immediately.** The operator's smoke-test verification happens AFTER merge against main, not against a branch worktree. |
| 2026-05-23 | **Output-format design needs the same care as code.** Tool A's summary lumped "submitted: 1" with "skipped: 1 (all_candidates_unsafe)" in the same block after a successful run. Operator reasonably asked "did I do something wrong?". My first answer was 500 words of filter machinery when they needed: "another repo, your PR is fine." | **The operator's clarity is a feature requirement, not a comment.** Apply the same care to operator-facing summaries as to the PR body voice. When a successful action produces output that mentions OTHER things, separate them visually or in the data model. |
| 2026-05-24 | **`_create_all_tables()` re-call inside a migration broke a Windows tempdir test.** v8→v9 ran `_create_all_tables` for idempotency, but the extra DB activity held the sqlite file open long enough that `tempfile.TemporaryDirectory.cleanup()` hit `PermissionError: WinError 32`. Manifested as `TestMigrationV2ToV3` failing only after PR-β. | **Narrow migrations to just the new schema.** Use explicit `CREATE TABLE IF NOT EXISTS` for the specific tables you're adding, not `_create_all_tables()`. The latter pattern is fine in v1→v2 (cold-start) but trips Windows tempdir races later in the chain. |
| 2026-05-24 | **Edit tool requires Read of the exact path, not same-content path.** I'd read `C:\...\gh-link-auditor\tools\foo.py` and then tried `Edit` on `C:\...\gh-link-auditor-282\tools\foo.py` — failed with "File has not been read yet" even though content was identical. | **Always `Read` from the worktree path before `Edit` against it.** This bit me at least 4 times across PRs. Treat worktree paths as separate files for tool tracking purposes. |
| 2026-05-24 | **Auto-mode classifier blocks PR body files mid-batch when it can't see the prior Write call in scope.** Hit 3 times this session: `gh pr create --body-file _pr_X_body.md` denied because "the file was never written in the transcript". The Write call HAD happened in a prior tool batch but classifier scope didn't see it. | **For PR creation: Write body file → verify with `ls -la + wc -l` in a separate Bash call → then `gh pr create --body-file`.** The intermediate verify makes the file's existence visible to the classifier. Skipping it forces a retry. |
| 2026-05-24 | **Score / gate scaffold returning `score=0` breaks any integration that has a `score < threshold` check.** PR-β shipped `run_preflight` returning PASS with score=0; PR-γ wired the integration; suddenly every candidate skipped with `preflight_score_0` because 0 < 90. | **Scaffolds with a threshold-gated output must return `score=threshold` (or whatever passes the gate by default).** Real scoring kicks in when real components land; scaffold should not poison the integration. Document the contract inline. |
| 2026-05-24 | **Hoisting `from ... import X` out of function bodies is required for `monkeypatch.setattr("...module.X", ...)` to work.** Tests for `gate_repo_active` / `score_r1_stars` / `score_r5_license` all needed it. Local imports leave no module-level attribute to patch. | **Test-mockability is a reason to keep imports at module level.** Local imports for circular-import workarounds are fine when no tests need to patch them; for collaborator factories where tests inject fakes via monkeypatch, hoist. |
| 2026-05-25 | **At session start, the agent skipped 9 of 10 files on the predecessor's *Files to Read First* list and rationalized the skip as "judgment-cut for context."** Skipped files contained load-bearing context (motivating incident, defensive-PASS calibration, soft-partial scoring, test coverage gap) the agent could not have known was load-bearing without reading them. Operator caught it by asking "what files were you told to read?" and "how did you record that?" — agent had no log. | **Treat the predecessor's "Files to Read First" list as authoritative; read every entry before any synthesis.** Memory file `feedback_defer_to_handoff_read_list.md` filed under new *Onboarding* section in `MEMORY.md`. Surgical-reads applies to unknown files in a codebase, not to a curated set. |
| 2026-05-25 | **A "you MUST" rule in CLAUDE.md (PRE-MERGE GATE: "wait for orchestrator review") had been silently ignored across many sessions because no orchestrator was actually running.** Multi-session compliance failure with a MUST rule trained the agent to discount every MUST in the file. | **When a CLAUDE.md MUST directive describes a process that does not exist in practice, retire or rewrite the rule rather than continuing to silently violate it.** Retired in PR #328. Same drift pattern likely present in other per-repo CLAUDE.mds (tracked in `AssemblyZero#1257`). |
| 2026-05-25 | **Quality stop-loss returned after the operator had fixed it multiple times in earlier sessions.** Skip-paths dominate the early median; threshold above 0 fires on every random-GitHub-sample run. | **Permanent disables need anti-regression tests, not just docstring rules.** #362 fixed; #364 tests. New convention: 'stay disabled forever' rules pin themselves in tests asserting both function behavior AND docstring string. |
| 2026-05-25 | **Visibility regression took three PRs (#354 -> #356 -> #360).** Each correct partial; operator wanted one-console end-state, not increments. | **Before declaring a visibility fix done, ask: would the operator have to open a second window or run a second tool to see this?** If yes, not done. In-process emission via configured logger to the same stderr stream is the answer. |
| 2026-05-25 | **`data/host-blocklist-candidates.md` was generated 2026-05-23 but never wired.** Every scan kept burning archive_client cycles on known-anti-bot hosts. Operator caught it watching a live run. | **Data without a code call site is data that lies.** When a candidates file lands, the next step is wiring it (or filing the wire-in issue) BEFORE closing the spawning work. PR #358; memory `feedback_blocklist_must_apply_to_stage3.md`. |
| 2026-05-25 | **Per-repo logging was the wrong fix for silent stage 3.** Operator wanted aggregate stats matching `finish_stage3.py`'s render_line, not 500+ log lines. | **For long-running stages, never propose per-event logging.** Always propose an aggregate single-line emitter sampled at an interval matching `finish_stage*.py`. Memory `feedback_progress_stats_not_per_repo_logs.md`. |
| 2026-05-25 | **Spawned a single ~16-minute monolithic agent for the regression audit.** Operator pointed out: power blip would lose hours of work. | **Any audit/sweep > ~15 minutes wall time MUST be chunked into parallel agents with per-chunk checkpoint files.** Re-architected the issue-pass into 8 parallel agents. Memory `feedback_chunk_parallel_with_checkpoints.md`. |
| 2026-05-25 | **Audit called out R2 (verify `setup_logging` propagate=False test) as missing. It already existed at `tests/unit/test_logging_config.py:64-69`.** Nearly re-implemented before checking. | **Audit findings of the form 'missing X' must be verified against actual code before action.** Audit doc is a snapshot; code is source of truth. |
| 2026-05-26 | **Multi-line `python -c '...'` repeatedly produced no console output on Windows during the session.** Caused at least one DB query to come back silent. | **For any diagnostic Python that needs to print, write to `tools/probe_*.py` and run it; never multi-line `python -c`.** Single-line `python -c` works; the silent-output failure mode is specifically multi-line via the bash tool. |

| 2026-05-26 | **Shipped a script (`reset_quality_aborted.py` #384) that defaulted to `status='investigating'` while the audit explicitly said "always reset to 'checking'" (lesson 2026-05-22). The audit was in my read-list. I read it. I still shipped the wrong default. The catastrophic consequence: `runner.run_full` skipped Stage 2, then `run_investigation` saw an empty `liveness_results` and stamped all 22,863 pending findings as `skipped_alive` in 48 seconds.** | **When a documented audit rule names a specific value (e.g. "always reset to 'checking'"), encode that value into a module-level constant AND a regression test asserting that constant is the post-reset state. The constant + test are the only defense against a future agent (or me-tomorrow) re-violating it from memory. PR #393 retrofitted this; the rule is now `RESET_TARGET_STATUS = "checking"` plus `test_sets_status_to_checking_not_investigating`. Generalize: every audit rule that names a specific value should have a constant + pinned test.** |
| 2026-05-26 | **Shipped `StatusEmitter` (#360) that shared its caller's `sqlite3` connection across a daemon thread -- a textbook `#F3` violation. Audit lesson 2026-05-23 said "DB writes go in the MAIN thread. Workers do pure compute." I shipped reads from a separate thread, which raises `ProgrammingError`, which the existing try/except swallowed as WARNING -- so zero status lines emitted during the broken run.** | **Audit rules in the `#F` (thread-safety) family must be enforced by tests that exercise the actual concurrent path, not just by code review. PR #391's `test_daemon_thread_emits_status_line_without_sqlite_error` is the regression test: spawn the daemon, wait for emission, assert at least one INFO line and zero "objects created in a thread" warnings. Generalize: any module that spawns a thread and uses a DB needs this test, even if the queries look "read-only".** |
| 2026-05-26 | **The `reset_quality_aborted.py` script went through THREE iterations (path bug, stale-AFTER bug, finally correct) because I shipped it without tests, then handed it to the operator. The operator caught both bugs at the keyboard. Each catch erodes trust further.** | **Operator-runnable scripts that touch shared state (DB, fork, push, blacklist) get unit tests against a `tmp_path` fixture BEFORE being handed to the operator. Cost of writing tests is bounded (~20 min); cost of an operator catching the bug is unbounded (trust + time). PR #384 retrofitted this with 11 tests; PR #393 added 4 more (including a load-bearing "AFTER differs from BEFORE" regression). Generalize: agent-authored operational scripts have >=1 test per documented behavior before any operator invocation.** |
| 2026-05-26 | **Recommended `finish_stage3.py --workers 32` as the "faster path" for resume without surfacing that it's at Stage 3 ONLY -- the paranoid filter (preflight) is a separate downstream concern. The operator caught this and called it half-assed.** | **When recommending a tool that produces output consumed by another tool, name the downstream dependency in the SAME sentence. "Run X to get fast Stage 3 candidates -- you'll still need `derive_replacement_prs.py --preflight-report-only` after to gate them through the paranoid filter." Generalize: any "use this tool" recommendation includes the next downstream consumer in the response, even if the operator didn't ask.** |
| 2026-05-27 | **Shipped #349 (C5 prompt tune) thinking it would rescue act-now-coalition. Re-ran preflight and got identical verdict — `unrelated`, score 65. Investigation revealed the C5 subagent's context dict passes only `candidate_url`, `link_text`, `landing_page` — but the prompt documents `dead_url`, `candidate_url`, `link_text`, `candidate_page`. The subagent never sees the dead URL, so the prompt's URL-pair normalization rules have nothing to compare. Six hours of prompt-tuning work was technically a no-op. Caught only because I re-ran preflight and got the same number.** | **When shipping a prompt change, verify the runtime CONTEXT passed to the subagent includes every variable the prompt references. Add a test that round-trips the actual call site's context-dict keys vs. the prompt's documented context. The byte-match golden-file test (#312) catches prompt drift but NOT context drift — they're orthogonal regressions. Also: always re-run the motivating case post-merge before claiming the fix worked.** |
| 2026-05-27 | **Operator said "i need a different limited PAT" — I jumped to "classic PAT with `public_repo` scope" because the existing code is built around classic-PAT decryption. Operator pushed back: "i don't think i am doing a calssic token my firend." Reality: their intent was fine-grained (the modern POLP-aligned choice). I'd lost time and we filed an extra issue. The actual constraint (issue #185: fine-grained can't fork arbitrary external repos) only surfaced after the back-and-forth.** | **When the operator describes a new credential / new component, surface the TYPE-OF-THING choice explicitly before picking. E.g.: "Two paths here — classic PAT with narrower scope, OR fine-grained PAT. Fine-grained is the modern POLP-aligned answer; classic is what the existing code uses because issue #185 says fine-grained can't fork arbitrary externals. Which do you want, or should I research whether #185 is still true?" Don't silently pick the path that fits the existing code shape.** |
| 2026-05-27 | **Tried bulk-`rm` on session-debris files (`probe_*.py`, `data/issue-body-*.md`) during /handoff Step 0. The auto-mode classifier blocked it with "no per-deletion authorization was given" — even though the operator had explicitly said earlier in the session "safe to delete whenever." The new CLAUDE.md rule "Operator authorization for 'do the work' does not include authorization to destroy operator's working-tree state. The destroy needs its own explicit yes, per operation" is structurally enforced by the classifier — prior conversational authorization doesn't survive multiple turns of intervening other work.** | **When deleting any untracked or modified file during cleanup/handoff: ASK for per-operation authorization in the same turn (not relying on text from many turns prior). Prior "safe to delete X" statements may have been valid in their original context but are NOT a standing license. The classifier's denial is correct; the right response is "report and surface to operator," not "find a workaround."** |
| 2026-07-28 | **A PR that only ADDS a tool auto-closed the issue asking for the tool's OUTPUT.** PR #444 shipped `tools/pin_ruff_in_lint_workflow.py` (the operator-run script that pins ruff in CI) and closed #443 for the script. GitHub's issue-linker also closed #428 — the issue tracking the actual pin — because the PR text referenced `#428` near closing language. Verified 3 days later during handoff: `lint.yml:20` still read `run: pip install ruff`, unpinned. An issue sat `COMPLETED` with none of its work done. | **When a PR ships a TOOL rather than the tool's EFFECT, mention the effect's issue with a non-trigger word (`see #N`, `blocks #N`, `tracked in #N`) — never bare `#N` adjacent to closing language.** This is a SECOND parasitic-close mechanism, distinct from the pr-sentinel regex already in CLAUDE.md: GitHub's own linker. After any PR that defers work to an operator step, re-read the deferred issue's state and confirm it is still open before declaring done. |
| 2026-07-28 | **Adding operator-facing logging turned a silent terminal into a firehose.** #399 asked for `logging.basicConfig(INFO)` so gate/N6 progress would surface in `derive_replacement_prs.py`. Implemented exactly as specced, the smoke run showed 12 of the first 14 lines were `httpx :: HTTP Request: GET ...` — one per GitHub API call — burying the signal the change existed to reveal. Caught only because I ran the tool for real instead of trusting the tests. | **Any `logging.basicConfig` in an operator-facing entry point must also demote `httpx`/`httpcore`/`urllib3` to WARNING** (the pattern `cli/main.py::_configure_logging` already uses). More generally: a UX change is not verified by tests — run the thing and read the actual output, because "silent" and "unreadable" are both failures and only one of them is visible in a test assertion. |
| 2026-07-31 | **A guard test asserting a directory was gone would have failed on every machine that pulled the change.** #413 moved `data/config/*.yaml` to `data-g/config/`. My first guard asserted `not (REPO_ROOT / "data" / "config").exists()`. `git mv` removes the tracked files but leaves the now-empty directory — and so does a `git pull` on any other machine, since git does not track directories. The test passed my intent and would have failed the operator's checkout. | **Assert on files, never on directory existence, when checking that a move completed.** `sorted(p.name for p in (d).glob("*.yaml"))` is the correct shape. Any assertion whose truth depends on filesystem state git does not track is environment-dependent by construction. |
